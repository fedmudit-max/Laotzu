/**
 * firebase.js — Remote auth / subscription verification (optional).
 * Wire up when you need server-verified entitlements.
 */

function initFirebase() {
    // No-op until Firebase is configured.
}
