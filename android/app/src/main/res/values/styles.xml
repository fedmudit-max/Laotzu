<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light only: Samsung force-dark inverts the WebView for a frame on launch and overlays.
         windowBackground stays King green so the handoff from splash → loadScreen is one color. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/king_splash</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/king_splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_king</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/king_splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowSplashScreenBackground">@color/king_splash</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_king</item>
        <item name="android:windowBackground">@color/king_splash</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
